* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, Helvetica, sans-serif;
    line-height: 1.6;
    color: #333;
}

.container {
    width: 90%;
    max-width: 1200px;
    margin: auto;
}

header {
    background: #111;
    color: white;
    padding: 20px 0;
}

header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo-section h1 {
    font-size: 2rem;
}

.phone-box {
    text-align: right;
}

.phone-box a {
    color: #f7c600;
    text-decoration: none;
    font-size: 1.6rem;
    font-weight: bold;
}

.hero {
    background: url('images/garage-hero.jpg') center center/cover;
    min-height: 550px;
}

.overlay {
    background: rgba(0,0,0,.6);
    min-height: 550px;
    display: flex;
    align-items: center;
}

.hero h2 {
    color: white;
    font-size: 3rem;
    max-width: 800px;
}

.hero p {
    color: white;
    font-size: 1.2rem;
    margin: 20px 0;
    max-width: 700px;
}

.btn {
    display: inline-block;
    background: #f7c600;
    color: black;
    text-decoration: none;
    padding: 15px 30px;
    border-radius: 5px;
    font-weight: bold;
}

.btn:hover {
    background: #e3b700;
}

section {
    padding: 70px 0;
}

h2 {
    margin-bottom: 25px;
    font-size: 2rem;
}

.about ul {
    margin-top: 20px;
    padding-left: 25px;
}

.services {
    background: #f4f4f4;
}

.service-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit,minmax(280px,1fr));
    gap: 25px;
}

.service-card {
    background: white;
    padding: 25px;
    border-radius: 8px;
    box-shadow: 0 2px 10px rgba(0,0,0,.1);
}

.service-card h3 {
    margin-bottom: 15px;
}

.gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit,minmax(250px,1fr));
    gap: 20px;
}

.gallery-grid img {
    width: 100%;
    height: 250px;
    object-fit: cover;
    border-radius: 8px;
}

.gallery-note {
    text-align: center;
    margin-top: 20px;
}

.reviews {
    background: #f4f4f4;
}

.review {
    background: white;
    padding: 25px;
    margin-bottom: 20px;
    border-left: 5px solid #f7c600;
    box-shadow: 0 2px 8px rgba(0,0,0,.08);
}

.areas {
    text-align: center;
}

.contact {
    background: #111;
    color: white;
    text-align: center;
}

.contact h2,
.contact h3 {
    margin-bottom: 20px;
}

.contact .btn {
    margin: 20px 0;
}

footer {
    background: black;
    color: white;
    text-align: center;
    padding: 20px;
}

@media(max-width:768px) {

    header .container {
        flex-direction: column;
        gap: 20px;
        text-align: center;
    }

    .phone-box {
        text-align: center;
    }

    .hero h2 {
        font-size: 2rem;
    }
}
